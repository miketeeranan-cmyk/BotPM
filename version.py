"""Stamped by the release workflow from the pushed git tag (v1.2.0 -> 1.2.0).

Left at 0.0.0 in the repo on purpose: a dev build should always look older than
any real release, so update checks from source never think they're current.
"""
VERSION = "0.0.0"
